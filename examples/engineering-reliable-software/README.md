# Chapter 1: Engineering Reliable Software

This project contains the complete Java 25 sources and tests used in Chapter 1.
It is independent of the companion projects for the other chapters.

Run `./gradlew test` from this directory. The included Gradle wrapper pins Gradle 9.6.1,
the build requests a Java 25 toolchain, and compilation enables all `javac` lint
warnings.
